import React from 'react';

const Dashboard = () => {
    return (
        <div>
            <h2>Dashboard</h2>
            <p>Manage your events, view guest lists, track budgets, and more.</p>
            {/* Add more features like creating events, managing guests, etc. */}
        </div>
    );
};

export default Dashboard;
